<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Spring MVC의 기본 설정을 활성화 -->
    <mvc:annotation-driven></mvc:annotation-driven>

<!--    &lt;!&ndash; 정적 자원 (CSS, JS, 이미지 등)을 /static 경로로 매핑 &ndash;&gt;
    <mvc:resources location="classpath:/static/" mapping="/static/**"></mvc:resources>-->

    <!-- com.wheelgo.controller 패키지를 스캔하여 컨트롤러를 자동으로 등록 -->
    <context:component-scan base-package="com.wheelgo.controller" use-default-filters="true">

        <!-- @Controller 애너테이션이 붙은 클래스를 자동 스캔에 포함 -->
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"></context:include-filter>

        <!-- @Service, @Repository 애너테이션이 붙은 클래스는 자동 스캔에서 제외 -->
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"></context:exclude-filter>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"></context:exclude-filter>

    </context:component-scan>

    <!-- 뷰 리졸버 설정: /WEB-INF/views/ 디렉토리의 JSP 파일을 뷰로 사용 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"></property>
        <property name="suffix" value=".jsp"></property>
        <property name="order" value="1"/>
    </bean>

</beans>
